# Stack prefix (used for container and volume names)
STACK_NAME=ts-trial

# Tailscale Configuration
TS_HOSTNAME=nginx-test
TS_AUTHKEY=tskey-client-xxxxxxxxxxxxxxxx

# Local port mapping for host access
NGINX_PORT=8080